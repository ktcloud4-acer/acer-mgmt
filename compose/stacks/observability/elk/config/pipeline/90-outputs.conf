output {
  kafka {
    id => "logs_to_kafka_buffer"
    bootstrap_servers => "${KAFKA_BOOTSTRAP_SERVERS:kafka:19092}"
    topic_id => "${LOGS_KAFKA_TOPIC:logs.buffered}"
    codec => json
    acks => "all"
    message_key => "%{[labels][team]}"
  }
}
